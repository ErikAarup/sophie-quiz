# KNOWN-ISSUES — WO-083 (sophie-quiz)

> Findings below the bounce bar, and §A items left out with the reason. Same format as KNOWN-ISSUES-WO-078.md.

| Date | Finding | Repro | Severity | Source |
|---|---|---|---|---|
| 2026-09-04 | The "Mer…" disclosure on the reveal and standings screens stays open once tapped until the screen changes (a new question, or the standings/reveal switch). Tapping "Mer…" again closes it. Nothing can be triggered by accident from it — both links still need their own tap and "Nollställ spelet" needs the word typed — but it is one more thing on screen while it is open. | Reveal screen → **Mer…** → tap outside. The two links stay. | Cosmetic; §3 puts the look of the disclosure below the bar. | builder |
| 2026-09-04 | The eyebrow on the reveal and standings screens ("FRÅGA 1 AV 10 · AVSLÖJAR") wraps to two lines at 390 px now that "Mer…" shares the topbar. It wrapped to three lines before this change, so it is a slight improvement, not a regression, but it is not one line. | Open the reveal screen at 390×844. | Cosmetic / pixel positions, explicitly below the bar per §3. | builder |
| 2026-09-04 | The data gate refuses a `data/quiz.json` that is not exactly ten questions of 150 s, on top of the four reasons AC1 names. §4 fixes the shape ("no change to … ten questions", "no change to 150 s") and the existing suite already asserted both, so the check preserves coverage rather than adding a rule — but if Erik ever deliberately wants nine lists or a different clock, `npm run deploy` will stop him until that test is updated. | Put nine slugs in `data/quiz.json`, run `npm test`. | Deliberate, logged so it is not a surprise. | builder |
| 2026-09-04 | The unit fixtures build question 1 from `data/bank.json` by the slug `most-populous-eu-countries` and carry their own alias table. That decouples `npm test` from `data/quiz.json` and `data/aliases.json` (AC2), but not from the bank: if `npm run sync-bank` ever brought back a bank without that slug, the fixture would fail by name ("fixture: data/bank.json has no list with slug …") rather than silently. Ten fixture slugs are pinned the same way. | Remove the list from `data/bank.json`, run `npm test`. | Low; the failure names itself. | builder |
| 2026-09-04 | `e2e/proof.spec.ts` (the WO-078 `npm run proof` walkthrough, not part of `npm run e2e`) still hard-codes answers from the EU list, so it assumes `quiz.json[0]` the way the unit and DO suites no longer do. §A2 named only the unit and DO tests, so it was left as it is. | Swap `data/quiz.json`, run `npm run proof`. | Test-coverage note; `npm test` and `npm run e2e` are the gates. | builder |
