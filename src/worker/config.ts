// Constants shared by the Worker entry, the DO and the tests. Kept out of index.ts because
// workerd only allows handlers/classes as named exports of the entry module.
export const GAME_NAME = 'sophie-2026';
