# KNOWN-ISSUES — WO-084 (sophie-quiz)

| Date | Finding | Repro | Severity | Source |
|---|---|---|---|---|
| 2026-09-04 | **AC4: the loser of a double "Rätta" says "Alla svar är redan rättade." as often as "Rättning pågår."** The work order asks the DO test to assert one ok + one `busy`. The race itself is closed — exactly one of two back-to-back `grade` commands is ever accepted, where before both were — but which refusal the second one gets is a matter of microseconds on the exact-hit path: with no model call there is nothing to wait for, so the first grade can settle (phase `reveal`) before the second command reduces, and the reducer then answers `done` / "Alla svar är redan rättade." instead of `busy` / "Rättning pågår.". Both are correct, both are shown as a toast, and neither grades twice. The DO test and the e2e accept either code and assert exactly one ok + one refusal. | `test/do/game.test.ts` "AC4: two grade commands back-to-back…"; `e2e/team-count.spec.ts` "AC4: …" | low (wording of a refusal, not behaviour) | builder |
| 2026-09-04 | **The "Går inte att ändra när spelet startat" wording is only ever seen through the server's refusal toast, not on a disabled control.** The stepper is rendered on the first lobby only; once question 1 has started there is no lobby screen showing it, and later lobbies (question 2–10) hide it rather than showing a permanently dead control. AC1 allows "hidden or disabled"; this is the hidden half. The refusal message is still produced and tested (unit + DO). | `src/client/admin.ts` `lobbyScreen(key, first)` | low (taste call, ledgered) | builder |
| 2026-09-04 | **Twelve tiles scroll on a 390×844 phone.** At 9–12 teams the player's tile grid uses a smaller tile (`.tiles.many`, 72 px / 30 px) and still pushes the "Fel lag?" footer just below the fold. Tapping is unaffected. Above 12 is out of scope by §4. | `proof/wo-084/6-player-tiles-12.png` | cosmetic | builder |
